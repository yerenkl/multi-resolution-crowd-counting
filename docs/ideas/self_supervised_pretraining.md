# Idea: Self-Supervised Pretraining on Unlabeled Crowds

Pretrain encoder with HR/LR consistency objectives on unlabeled crowd images, then fine-tune on NWPU labels.

Goal:
- Learn resolution-robust representations before supervised training

Dependency:
- Requires availability of suitable unlabeled data
